function findMovie(req, res) {
    var data = req.params;
    // console.log(data);
    var id = data.id;
    
    // query goes here
    
    // save query to json object

    // send json object
    res.send();
}

function findShow(req, res) {
    var data = req.params;
    // console.log(data);
    var id = data.id;
    
    // query goes here

    // save query to json object

    // send json object
    res.send();
}

function search(req, res) {

    res.send();
}
module.exports = app;